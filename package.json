{
  "name": "sbti-test-static",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone multilingual static SBTI website",
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build.mjs",
    "build": "npm run build:pages && vite build",
    "dev": "vite",
    "preview": "vite preview",
    "sync": "node scripts/sync-source.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}
